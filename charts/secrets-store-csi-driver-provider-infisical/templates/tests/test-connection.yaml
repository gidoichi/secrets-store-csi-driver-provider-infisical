apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "secrets-store-csi-driver-provider-infisical.fullname" . }}-test-connection"
  labels:
    {{- include "secrets-store-csi-driver-provider-infisical.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "secrets-store-csi-driver-provider-infisical.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
